import type { PropTypes } from '../App'

const EditPage = ({singleCarState, setSingleCarState}
  : Pick<PropTypes, 'singleCarState' | 'setSingleCarState'>) => {
  return (
    <div>EditPage</div>
  )
}

export default EditPage